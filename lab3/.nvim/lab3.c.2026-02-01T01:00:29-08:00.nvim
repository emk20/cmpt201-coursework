#define _POSIX_C_SOURCE_ 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
  char* buff = NULL;
  size_t size = 0;

  while(1) {
    printf("Enter input:");

    ssize_t num_char = getline(&buff, &size, stdin);
  
  }
  return 0;
}

